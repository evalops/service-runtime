# Service Runtime

Canonical source for this repository now lives in [`evalops/mono`](https://github.com/evalops/mono) at `mono (retired standalone)`.

This tree was imported from `retired` in evalops/monon/a.

Do not land new product work here. Open pull requests against `evalops/mono`.
